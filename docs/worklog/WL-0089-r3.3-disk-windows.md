<!-- INITECH CONFIDENTIAL -- INTERNAL USE ONLY -->

# WL-0089 -- R3.3 THE DISK WINDOWS: THE VOLUME OPENS, THE KERNEL STOPS EATING ITSELF

**Date:** 2026-08-25 (same orchestrated sitting as WL-0088)
**Beads:** initech-uzjc (KERNEL_SECTORS bump) + initech-tdnl.10 (R3.3 disk
windows) -- both CLOSED. Filed: the kernel-.bin-prerequisite skipped-oracle
hazard, the bounce-policy design gate, and the same-tenant title-click
raise gap (bug, P2).
**Commits:** `e3336b5` (uzjc) -> `8b984e2` (tdnl.10 core + the kstart find) ->
`0129a3e` (tdnl.10 emu wiring + harness chords).
**Closing certificate:** `make clean && make test` ALL GREEN -- see Acceptance.

## Context

Continuation of the WL-0088 sitting under the same operator directive (opus
lanes, orchestrator grades everything first-person, motion evidence
mandatory). The R3.2 close left the tenants kernel ~4KiB from its window;
uzjc had to land before tdnl.10 could fit.

## What changed

1. **The kernel window is at its ratified ceiling (`e3336b5`).**
   KERNEL_SECTORS 352->384: NOT a round number -- 384*512 = 0x30000 =
   KERNEL_BOUNCE_CAP, so the real-mode bounce now ends EXACTLY at
   PROGRAM_BASE (0x40000) exclusive (design K2). IMG_SECTORS 384->448 (the
   whole-cylinder rule; terminal). The eight-bump-old hand-kept
   Makefile<->stage2 equality is now MECHANICAL: the Makefile passes
   -DMK_KERNEL_SECTORS and stage2 %errors on drift OR absence; the decorative
   KERNEL_BOUNCE_CAP equate became a load-bearing %error. Mutants proven at
   the new base in both drift directions. Byte-identical double rebuild.

2. **THE FIND: kstart.asm was erasing the kernel's own tail (`8b984e2`).**
   A THIRD copy of KERNEL_SECTORS -- in os/milton/kstart.asm, driving where
   the runway-clearing stosd STARTS -- was missed by uzjc and stayed at 352.
   Latent for as long as every kernel stayed under 352 sectors; the moment
   tdnl.10 grew the image past that line, the kernel zeroed its own .rodata
   tail in place: serial_puthex32 printed NULs (digit table read as zero),
   the banner's second line was garbage, the boot stalled with NO fault and
   NO marker -- the quietly-wrong failure Rule 2 exists to forbid. Diagnosed
   by QMP pmemsave diffed against the padded .bin (memory zero from exactly
   0x2C000); fixed at root: kstart takes the same -DMK_KERNEL_SECTORS %error
   guard, ALL THREE copies now locked (bd memory
   kstart-runway-erase-trap-2026-08-25). Consequence: uzjc's "46KiB headroom"
   was illusory (everything past 352 sectors was being wiped); the REAL
   post-R3.3 headroom is ~6.9KiB at the bounce ceiling -- the bounce-policy
   design bead is NEAR, not far.

3. **R3.3 core (`8b984e2`).** os/flair/finder_windows.{h,c}: double-click
   the volume opens "Drive A Files" -- a real Finder-owned WindowRecord
   enumerating the real FAT12 root through a finder_fs_t binding kmain wires
   to fat12_read_dir/mkdir (os/flair spells no FAT12_ERR_*; attr constants
   proven equal by _Static_assert in the one TU with both in scope;
   callback-lifetime contract honored -- fields copied out during the
   callback). Spatial singleton keyed by dir_start; close = DisposeWindow +
   kind=4 view record (origin + view bits) through the locked DB codec;
   Clean Up row-major snap; New Folder creates a REAL directory (NEWFOLD,
   deterministic collision ladder); Ctrl-chords reach the spine via
   finder_cmd_key_lookup (single dispatch path preserved; tdnl.12 replaces
   the lookup with real MenuKey). THREE new hand-authored 32x32 strikes
   (spec/assets/finder_icons.h: folder / document / application, sibling
   file so the R3.2 locked bytes are untouched; transcriber method per the
   protocolized workaround). Host oracle test-finder-windows: 157 checks
   incl. a stale-pointer honesty leg; mutants SINGLETON_DUP /
   VOLLABEL_SHOWN / CLEANUP_UNSORTED RED. The R3.2 gate's FINDER-OPEN-VOLUME
   NYI leg re-keyed strictly stronger (the window now opens: win=0 n=4).

4. **R3.3 booted oracle + clips (`0129a3e`).** THE HARNESS LEARNED CHORDS:
   QMP send-key takes an array pressed together, so "ctrl-n" is ONE command
   -- token_to_qcode gains modifiers, qmp_send_key_chord lands, and a new
   k<chord> token in the MOUSE grammar gives one ordered input stream
   (chords can follow clicks). Spec buffers 256->1024 -- 256 was SILENTLY
   TRUNCATING long traces (a replayed prefix grades the wrong scene; found
   before it bit). test-flair-disk-windows: 5 legs (root-window pixel leg
   with every coordinate hand-derived from finder_windows.h constants + the
   strike maps; the singleton serial chain; window-drag view persistence
   across a REBOOT; Ctrl-N + mtools differential proving the directory is
   real) + mutant images asserting exact serial divergences in both
   directions + a Bochs leg with a no-window-after-halt tooth. Clips
   folder_nav / window_drag_persist / new_folder: byte-identical repro x3,
   orchestrator frame-eyeballed (per-kind strikes, inactive-title dimming,
   NEWFOLD appearing on the next grid row), sent to the operator.
   A second RECORD image variant widens FINDER_DBLCLICK_TICKS (now
   #ifndef-guarded) because per-event PPM dumps exceed 20 ticks between
   clicks; default build byte-identical.

## Frictions / finds

- **The kstart runway-erase** (above) -- the sharpest lesson: when
  mechanizing a hand-kept constant pairing, grep for EVERY copy; a "pair"
  may be a triple.
- **Same-tenant background-window title click does not raise** (measured:
  FLAIR-DRAG fires, z-order unchanged -- DQ5 switches tenants, and two
  Finder windows share one). Period Macs raise on any background-window
  click. Filed as a P2 bug; tdnl.12 territory.
- Clean Up has NO emu-injectable input until the tdnl.12 menu bar
  (cmd_char 0 is period-correct); host-covered, stated loudly in the gate.
- Shift-click remains emu-unreachable (needs held modifiers across
  input-send-event, not send-key) -- bead 05rz unchanged.
- Kernel .bin recipes still lack the Makefile prerequisite (skipped-oracle
  hazard bead filed under uzjc's close) -- bit lane E mid-flight, caught by
  missing guard confirmation lines.

## Acceptance

- Certificates run first-person at every seam: after uzjc ALL GREEN 347+102;
  after the R3.3 core ALL GREEN 349+102; closing certificate for the sitting
  ALL GREEN **349 host + 105 emu** (test-flair-disk-windows, -mutant, -bochs
  join the vector).
- Boot-frame invariant held at every landing (tenants boot dump bit-identical
  until a slice deliberately changes it -- none did).
- All new goldens mutation-proven; both R3.3 mutants assert exact serial
  divergences, not mere absence.

## Pointers

- Next per the F5-1 order: **initech-tdnl.12** (the real Finder menu bar --
  band-2-at-rest re-key is DELIBERATE and needs the operator Law-4 clip
  sign-off) with the raise-gap bug adjacent; then tdnl.26 -> tdnl.11 ->
  tdnl.13 -> tdnl.14.
- The bounce-policy design gate is NEAR (~6.9KiB headroom at the ceiling).
- Clips: build/clips/{folder_nav,window_drag_persist,new_folder}.{gif,mp4}.
